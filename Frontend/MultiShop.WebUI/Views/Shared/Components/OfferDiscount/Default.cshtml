@model List<OfferDiscountModel>

@if (Model != null && Model.Any())
{
    <div class="container-fluid pt-5 pb-3">
        <div class="row px-xl-5">
            @foreach (var specialOffer in Model)
            {
                <div class="col-md-6">
                    <div class="product-offer mb-30" style="height: 300px;">
                        <img class="img-fluid" src="@specialOffer.ImageUrl" alt="@specialOffer.Title">
                        <div class="offer-text">
                            <h6 class="text-white text-uppercase">@specialOffer.Title</h6>
                            <h3 class="text-white mb-3">@specialOffer.SubTitle</h3>
                            <a href="" class="btn btn-primary">Şimdi Alışveriş Yapın</a>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
}
